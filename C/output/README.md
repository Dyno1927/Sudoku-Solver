# output

Where `solution.txt` lands after a successful solve. Written by Solver.c,
read back by Fetcher/pipeline.py, then the answers get posted to the site.
Generated every run, so it's gitignored — the dir stays here so the solver
always has somewhere to write to.